# Latest MathJax benchmark — 500 formulas

<!-- Generated by `npm run bench:generate` (scripts/generate-benchmarks.mjs); do not edit by hand. -->
<!-- Composition: 500 formulas = 200 unique inline + 100 repeated inline + 100 unique display + 50 repeated display + 25 macro + 25 invalid (intentional). -->
<!-- Setup: copy benchmark-preamble.tex into the vault and set the plugin's Preamble file setting to its vault path; keep other settings at their defaults. -->

## Unique inline (200)

Each source below is distinct, so every render is a cache miss and may load New Computer Modern glyph chunks on first sight.

The bound in item 1 reads $f_{1}(x) = 1x^{2} + 2x + 2$. Rewriting item 2 yields $\sqrt{x^{2} + y_{2}^{2}} \le 6$. One verifies item 3: $\frac{4}{7} + \frac{q_{3}}{5}$. For comparison, item 4 states that $\sum_{k=1}^{13} k^{3}$. Segment 5 gives $\int_{0}^{\pi/6} \sin^{3}(t)\,dt$.

The bound in item 6 reads $\mathcal{F}_{6} \subset \mathbb{R}^{8}$. Rewriting item 7 yields $A_{7} \xrightarrow{\ \phi_{7}\ } B_{8}$. One verifies item 8: $\begin{bmatrix} 8 & 9 \\ 16 & 11\end{bmatrix}^{-1}$. For comparison, item 9 states that $e^{-9x}\cos(9x)$. Segment 10 gives $\lim_{n \to \infty} n^{-10} \cdot 11$.

The bound in item 11 reads $f_{11}(x) = 11x^{2} + 22x + 12$. Rewriting item 12 yields $\sqrt{x^{2} + y_{12}^{2}} \le 16$. One verifies item 13: $\frac{14}{27} + \frac{q_{13}}{15}$. For comparison, item 14 states that $\sum_{k=1}^{23} k^{4}$. Segment 15 gives $\int_{0}^{\pi/16} \sin^{5}(t)\,dt$.

The bound in item 16 reads $\mathcal{F}_{16} \subset \mathbb{R}^{18}$. Rewriting item 17 yields $A_{17} \xrightarrow{\ \phi_{17}\ } B_{18}$. One verifies item 18: $\begin{bmatrix} 18 & 19 \\ 36 & 21\end{bmatrix}^{-1}$. For comparison, item 19 states that $e^{-19x}\cos(19x)$. Segment 20 gives $\lim_{n \to \infty} n^{-20} \cdot 21$.

The bound in item 21 reads $f_{21}(x) = 21x^{2} + 42x + 22$. Rewriting item 22 yields $\sqrt{x^{2} + y_{22}^{2}} \le 26$. One verifies item 23: $\frac{24}{47} + \frac{q_{23}}{25}$. For comparison, item 24 states that $\sum_{k=1}^{33} k^{2}$. Segment 25 gives $\int_{0}^{\pi/26} \sin^{3}(t)\,dt$.

The bound in item 26 reads $\mathcal{F}_{26} \subset \mathbb{R}^{28}$. Rewriting item 27 yields $A_{27} \xrightarrow{\ \phi_{27}\ } B_{28}$. One verifies item 28: $\begin{bmatrix} 28 & 29 \\ 56 & 31\end{bmatrix}^{-1}$. For comparison, item 29 states that $e^{-29x}\cos(29x)$. Segment 30 gives $\lim_{n \to \infty} n^{-30} \cdot 31$.

The bound in item 31 reads $f_{31}(x) = 31x^{2} + 62x + 32$. Rewriting item 32 yields $\sqrt{x^{2} + y_{32}^{2}} \le 36$. One verifies item 33: $\frac{34}{67} + \frac{q_{33}}{35}$. For comparison, item 34 states that $\sum_{k=1}^{43} k^{3}$. Segment 35 gives $\int_{0}^{\pi/36} \sin^{5}(t)\,dt$.

The bound in item 36 reads $\mathcal{F}_{36} \subset \mathbb{R}^{38}$. Rewriting item 37 yields $A_{37} \xrightarrow{\ \phi_{37}\ } B_{38}$. One verifies item 38: $\begin{bmatrix} 38 & 39 \\ 76 & 41\end{bmatrix}^{-1}$. For comparison, item 39 states that $e^{-39x}\cos(39x)$. Segment 40 gives $\lim_{n \to \infty} n^{-40} \cdot 41$.

The bound in item 41 reads $f_{41}(x) = 41x^{2} + 82x + 42$. Rewriting item 42 yields $\sqrt{x^{2} + y_{42}^{2}} \le 46$. One verifies item 43: $\frac{44}{87} + \frac{q_{43}}{45}$. For comparison, item 44 states that $\sum_{k=1}^{53} k^{4}$. Segment 45 gives $\int_{0}^{\pi/46} \sin^{3}(t)\,dt$.

The bound in item 46 reads $\mathcal{F}_{46} \subset \mathbb{R}^{48}$. Rewriting item 47 yields $A_{47} \xrightarrow{\ \phi_{47}\ } B_{48}$. One verifies item 48: $\begin{bmatrix} 48 & 49 \\ 96 & 51\end{bmatrix}^{-1}$. For comparison, item 49 states that $e^{-49x}\cos(49x)$. Segment 50 gives $\lim_{n \to \infty} n^{-50} \cdot 51$.

The bound in item 51 reads $f_{51}(x) = 51x^{2} + 102x + 52$. Rewriting item 52 yields $\sqrt{x^{2} + y_{52}^{2}} \le 56$. One verifies item 53: $\frac{54}{107} + \frac{q_{53}}{55}$. For comparison, item 54 states that $\sum_{k=1}^{63} k^{2}$. Segment 55 gives $\int_{0}^{\pi/56} \sin^{5}(t)\,dt$.

The bound in item 56 reads $\mathcal{F}_{56} \subset \mathbb{R}^{58}$. Rewriting item 57 yields $A_{57} \xrightarrow{\ \phi_{57}\ } B_{58}$. One verifies item 58: $\begin{bmatrix} 58 & 59 \\ 116 & 61\end{bmatrix}^{-1}$. For comparison, item 59 states that $e^{-59x}\cos(59x)$. Segment 60 gives $\lim_{n \to \infty} n^{-60} \cdot 61$.

The bound in item 61 reads $f_{61}(x) = 61x^{2} + 122x + 62$. Rewriting item 62 yields $\sqrt{x^{2} + y_{62}^{2}} \le 66$. One verifies item 63: $\frac{64}{127} + \frac{q_{63}}{65}$. For comparison, item 64 states that $\sum_{k=1}^{73} k^{3}$. Segment 65 gives $\int_{0}^{\pi/66} \sin^{3}(t)\,dt$.

The bound in item 66 reads $\mathcal{F}_{66} \subset \mathbb{R}^{68}$. Rewriting item 67 yields $A_{67} \xrightarrow{\ \phi_{67}\ } B_{68}$. One verifies item 68: $\begin{bmatrix} 68 & 69 \\ 136 & 71\end{bmatrix}^{-1}$. For comparison, item 69 states that $e^{-69x}\cos(69x)$. Segment 70 gives $\lim_{n \to \infty} n^{-70} \cdot 71$.

The bound in item 71 reads $f_{71}(x) = 71x^{2} + 142x + 72$. Rewriting item 72 yields $\sqrt{x^{2} + y_{72}^{2}} \le 76$. One verifies item 73: $\frac{74}{147} + \frac{q_{73}}{75}$. For comparison, item 74 states that $\sum_{k=1}^{83} k^{4}$. Segment 75 gives $\int_{0}^{\pi/76} \sin^{5}(t)\,dt$.

The bound in item 76 reads $\mathcal{F}_{76} \subset \mathbb{R}^{78}$. Rewriting item 77 yields $A_{77} \xrightarrow{\ \phi_{77}\ } B_{78}$. One verifies item 78: $\begin{bmatrix} 78 & 79 \\ 156 & 81\end{bmatrix}^{-1}$. For comparison, item 79 states that $e^{-79x}\cos(79x)$. Segment 80 gives $\lim_{n \to \infty} n^{-80} \cdot 81$.

The bound in item 81 reads $f_{81}(x) = 81x^{2} + 162x + 82$. Rewriting item 82 yields $\sqrt{x^{2} + y_{82}^{2}} \le 86$. One verifies item 83: $\frac{84}{167} + \frac{q_{83}}{85}$. For comparison, item 84 states that $\sum_{k=1}^{93} k^{2}$. Segment 85 gives $\int_{0}^{\pi/86} \sin^{3}(t)\,dt$.

The bound in item 86 reads $\mathcal{F}_{86} \subset \mathbb{R}^{88}$. Rewriting item 87 yields $A_{87} \xrightarrow{\ \phi_{87}\ } B_{88}$. One verifies item 88: $\begin{bmatrix} 88 & 89 \\ 176 & 91\end{bmatrix}^{-1}$. For comparison, item 89 states that $e^{-89x}\cos(89x)$. Segment 90 gives $\lim_{n \to \infty} n^{-90} \cdot 91$.

The bound in item 91 reads $f_{91}(x) = 91x^{2} + 182x + 92$. Rewriting item 92 yields $\sqrt{x^{2} + y_{92}^{2}} \le 96$. One verifies item 93: $\frac{94}{187} + \frac{q_{93}}{95}$. For comparison, item 94 states that $\sum_{k=1}^{103} k^{3}$. Segment 95 gives $\int_{0}^{\pi/96} \sin^{5}(t)\,dt$.

The bound in item 96 reads $\mathcal{F}_{96} \subset \mathbb{R}^{98}$. Rewriting item 97 yields $A_{97} \xrightarrow{\ \phi_{97}\ } B_{98}$. One verifies item 98: $\begin{bmatrix} 98 & 99 \\ 196 & 101\end{bmatrix}^{-1}$. For comparison, item 99 states that $e^{-99x}\cos(99x)$. Segment 100 gives $\lim_{n \to \infty} n^{-100} \cdot 101$.

The bound in item 101 reads $f_{101}(x) = 101x^{2} + 202x + 102$. Rewriting item 102 yields $\sqrt{x^{2} + y_{102}^{2}} \le 106$. One verifies item 103: $\frac{104}{207} + \frac{q_{103}}{105}$. For comparison, item 104 states that $\sum_{k=1}^{113} k^{4}$. Segment 105 gives $\int_{0}^{\pi/106} \sin^{3}(t)\,dt$.

The bound in item 106 reads $\mathcal{F}_{106} \subset \mathbb{R}^{108}$. Rewriting item 107 yields $A_{107} \xrightarrow{\ \phi_{107}\ } B_{108}$. One verifies item 108: $\begin{bmatrix} 108 & 109 \\ 216 & 111\end{bmatrix}^{-1}$. For comparison, item 109 states that $e^{-109x}\cos(109x)$. Segment 110 gives $\lim_{n \to \infty} n^{-110} \cdot 111$.

The bound in item 111 reads $f_{111}(x) = 111x^{2} + 222x + 112$. Rewriting item 112 yields $\sqrt{x^{2} + y_{112}^{2}} \le 116$. One verifies item 113: $\frac{114}{227} + \frac{q_{113}}{115}$. For comparison, item 114 states that $\sum_{k=1}^{123} k^{2}$. Segment 115 gives $\int_{0}^{\pi/116} \sin^{5}(t)\,dt$.

The bound in item 116 reads $\mathcal{F}_{116} \subset \mathbb{R}^{118}$. Rewriting item 117 yields $A_{117} \xrightarrow{\ \phi_{117}\ } B_{118}$. One verifies item 118: $\begin{bmatrix} 118 & 119 \\ 236 & 121\end{bmatrix}^{-1}$. For comparison, item 119 states that $e^{-119x}\cos(119x)$. Segment 120 gives $\lim_{n \to \infty} n^{-120} \cdot 121$.

The bound in item 121 reads $f_{121}(x) = 121x^{2} + 242x + 122$. Rewriting item 122 yields $\sqrt{x^{2} + y_{122}^{2}} \le 126$. One verifies item 123: $\frac{124}{247} + \frac{q_{123}}{125}$. For comparison, item 124 states that $\sum_{k=1}^{133} k^{3}$. Segment 125 gives $\int_{0}^{\pi/126} \sin^{3}(t)\,dt$.

The bound in item 126 reads $\mathcal{F}_{126} \subset \mathbb{R}^{128}$. Rewriting item 127 yields $A_{127} \xrightarrow{\ \phi_{127}\ } B_{128}$. One verifies item 128: $\begin{bmatrix} 128 & 129 \\ 256 & 131\end{bmatrix}^{-1}$. For comparison, item 129 states that $e^{-129x}\cos(129x)$. Segment 130 gives $\lim_{n \to \infty} n^{-130} \cdot 131$.

The bound in item 131 reads $f_{131}(x) = 131x^{2} + 262x + 132$. Rewriting item 132 yields $\sqrt{x^{2} + y_{132}^{2}} \le 136$. One verifies item 133: $\frac{134}{267} + \frac{q_{133}}{135}$. For comparison, item 134 states that $\sum_{k=1}^{143} k^{4}$. Segment 135 gives $\int_{0}^{\pi/136} \sin^{5}(t)\,dt$.

The bound in item 136 reads $\mathcal{F}_{136} \subset \mathbb{R}^{138}$. Rewriting item 137 yields $A_{137} \xrightarrow{\ \phi_{137}\ } B_{138}$. One verifies item 138: $\begin{bmatrix} 138 & 139 \\ 276 & 141\end{bmatrix}^{-1}$. For comparison, item 139 states that $e^{-139x}\cos(139x)$. Segment 140 gives $\lim_{n \to \infty} n^{-140} \cdot 141$.

The bound in item 141 reads $f_{141}(x) = 141x^{2} + 282x + 142$. Rewriting item 142 yields $\sqrt{x^{2} + y_{142}^{2}} \le 146$. One verifies item 143: $\frac{144}{287} + \frac{q_{143}}{145}$. For comparison, item 144 states that $\sum_{k=1}^{153} k^{2}$. Segment 145 gives $\int_{0}^{\pi/146} \sin^{3}(t)\,dt$.

The bound in item 146 reads $\mathcal{F}_{146} \subset \mathbb{R}^{148}$. Rewriting item 147 yields $A_{147} \xrightarrow{\ \phi_{147}\ } B_{148}$. One verifies item 148: $\begin{bmatrix} 148 & 149 \\ 296 & 151\end{bmatrix}^{-1}$. For comparison, item 149 states that $e^{-149x}\cos(149x)$. Segment 150 gives $\lim_{n \to \infty} n^{-150} \cdot 151$.

The bound in item 151 reads $f_{151}(x) = 151x^{2} + 302x + 152$. Rewriting item 152 yields $\sqrt{x^{2} + y_{152}^{2}} \le 156$. One verifies item 153: $\frac{154}{307} + \frac{q_{153}}{155}$. For comparison, item 154 states that $\sum_{k=1}^{163} k^{3}$. Segment 155 gives $\int_{0}^{\pi/156} \sin^{5}(t)\,dt$.

The bound in item 156 reads $\mathcal{F}_{156} \subset \mathbb{R}^{158}$. Rewriting item 157 yields $A_{157} \xrightarrow{\ \phi_{157}\ } B_{158}$. One verifies item 158: $\begin{bmatrix} 158 & 159 \\ 316 & 161\end{bmatrix}^{-1}$. For comparison, item 159 states that $e^{-159x}\cos(159x)$. Segment 160 gives $\lim_{n \to \infty} n^{-160} \cdot 161$.

The bound in item 161 reads $f_{161}(x) = 161x^{2} + 322x + 162$. Rewriting item 162 yields $\sqrt{x^{2} + y_{162}^{2}} \le 166$. One verifies item 163: $\frac{164}{327} + \frac{q_{163}}{165}$. For comparison, item 164 states that $\sum_{k=1}^{173} k^{4}$. Segment 165 gives $\int_{0}^{\pi/166} \sin^{3}(t)\,dt$.

The bound in item 166 reads $\mathcal{F}_{166} \subset \mathbb{R}^{168}$. Rewriting item 167 yields $A_{167} \xrightarrow{\ \phi_{167}\ } B_{168}$. One verifies item 168: $\begin{bmatrix} 168 & 169 \\ 336 & 171\end{bmatrix}^{-1}$. For comparison, item 169 states that $e^{-169x}\cos(169x)$. Segment 170 gives $\lim_{n \to \infty} n^{-170} \cdot 171$.

The bound in item 171 reads $f_{171}(x) = 171x^{2} + 342x + 172$. Rewriting item 172 yields $\sqrt{x^{2} + y_{172}^{2}} \le 176$. One verifies item 173: $\frac{174}{347} + \frac{q_{173}}{175}$. For comparison, item 174 states that $\sum_{k=1}^{183} k^{2}$. Segment 175 gives $\int_{0}^{\pi/176} \sin^{5}(t)\,dt$.

The bound in item 176 reads $\mathcal{F}_{176} \subset \mathbb{R}^{178}$. Rewriting item 177 yields $A_{177} \xrightarrow{\ \phi_{177}\ } B_{178}$. One verifies item 178: $\begin{bmatrix} 178 & 179 \\ 356 & 181\end{bmatrix}^{-1}$. For comparison, item 179 states that $e^{-179x}\cos(179x)$. Segment 180 gives $\lim_{n \to \infty} n^{-180} \cdot 181$.

The bound in item 181 reads $f_{181}(x) = 181x^{2} + 362x + 182$. Rewriting item 182 yields $\sqrt{x^{2} + y_{182}^{2}} \le 186$. One verifies item 183: $\frac{184}{367} + \frac{q_{183}}{185}$. For comparison, item 184 states that $\sum_{k=1}^{193} k^{3}$. Segment 185 gives $\int_{0}^{\pi/186} \sin^{3}(t)\,dt$.

The bound in item 186 reads $\mathcal{F}_{186} \subset \mathbb{R}^{188}$. Rewriting item 187 yields $A_{187} \xrightarrow{\ \phi_{187}\ } B_{188}$. One verifies item 188: $\begin{bmatrix} 188 & 189 \\ 376 & 191\end{bmatrix}^{-1}$. For comparison, item 189 states that $e^{-189x}\cos(189x)$. Segment 190 gives $\lim_{n \to \infty} n^{-190} \cdot 191$.

The bound in item 191 reads $f_{191}(x) = 191x^{2} + 382x + 192$. Rewriting item 192 yields $\sqrt{x^{2} + y_{192}^{2}} \le 196$. One verifies item 193: $\frac{194}{387} + \frac{q_{193}}{195}$. For comparison, item 194 states that $\sum_{k=1}^{203} k^{4}$. Segment 195 gives $\int_{0}^{\pi/196} \sin^{5}(t)\,dt$.

The bound in item 196 reads $\mathcal{F}_{196} \subset \mathbb{R}^{198}$. Rewriting item 197 yields $A_{197} \xrightarrow{\ \phi_{197}\ } B_{198}$. One verifies item 198: $\begin{bmatrix} 198 & 199 \\ 396 & 201\end{bmatrix}^{-1}$. For comparison, item 199 states that $e^{-199x}\cos(199x)$. Segment 200 gives $\lim_{n \to \infty} n^{-200} \cdot 201$.

## Repeated inline (100)

The same handful of sources repeats throughout, so the first instance pays for typesetting and later instances exercise the formula cache.

The bound in item 1 reads $e^{i\pi} = -1$. Rewriting item 2 yields $a^{2} + b^{2} = c^{2}$. One verifies item 3: $\hat{H}\,\Psi = E\,\Psi$. For comparison, item 4 states that $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. Segment 5 gives $e^{i\pi} = -1$.

The bound in item 6 reads $a^{2} + b^{2} = c^{2}$. Rewriting item 7 yields $\hat{H}\,\Psi = E\,\Psi$. One verifies item 8: $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. For comparison, item 9 states that $e^{i\pi} = -1$. Segment 10 gives $a^{2} + b^{2} = c^{2}$.

The bound in item 11 reads $\hat{H}\,\Psi = E\,\Psi$. Rewriting item 12 yields $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. One verifies item 13: $e^{i\pi} = -1$. For comparison, item 14 states that $a^{2} + b^{2} = c^{2}$. Segment 15 gives $\hat{H}\,\Psi = E\,\Psi$.

The bound in item 16 reads $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. Rewriting item 17 yields $e^{i\pi} = -1$. One verifies item 18: $a^{2} + b^{2} = c^{2}$. For comparison, item 19 states that $\hat{H}\,\Psi = E\,\Psi$. Segment 20 gives $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$.

The bound in item 21 reads $e^{i\pi} = -1$. Rewriting item 22 yields $a^{2} + b^{2} = c^{2}$. One verifies item 23: $\hat{H}\,\Psi = E\,\Psi$. For comparison, item 24 states that $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. Segment 25 gives $e^{i\pi} = -1$.

The bound in item 26 reads $a^{2} + b^{2} = c^{2}$. Rewriting item 27 yields $\hat{H}\,\Psi = E\,\Psi$. One verifies item 28: $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. For comparison, item 29 states that $e^{i\pi} = -1$. Segment 30 gives $a^{2} + b^{2} = c^{2}$.

The bound in item 31 reads $\hat{H}\,\Psi = E\,\Psi$. Rewriting item 32 yields $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. One verifies item 33: $e^{i\pi} = -1$. For comparison, item 34 states that $a^{2} + b^{2} = c^{2}$. Segment 35 gives $\hat{H}\,\Psi = E\,\Psi$.

The bound in item 36 reads $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. Rewriting item 37 yields $e^{i\pi} = -1$. One verifies item 38: $a^{2} + b^{2} = c^{2}$. For comparison, item 39 states that $\hat{H}\,\Psi = E\,\Psi$. Segment 40 gives $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$.

The bound in item 41 reads $e^{i\pi} = -1$. Rewriting item 42 yields $a^{2} + b^{2} = c^{2}$. One verifies item 43: $\hat{H}\,\Psi = E\,\Psi$. For comparison, item 44 states that $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. Segment 45 gives $e^{i\pi} = -1$.

The bound in item 46 reads $a^{2} + b^{2} = c^{2}$. Rewriting item 47 yields $\hat{H}\,\Psi = E\,\Psi$. One verifies item 48: $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. For comparison, item 49 states that $e^{i\pi} = -1$. Segment 50 gives $a^{2} + b^{2} = c^{2}$.

The bound in item 51 reads $\hat{H}\,\Psi = E\,\Psi$. Rewriting item 52 yields $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. One verifies item 53: $e^{i\pi} = -1$. For comparison, item 54 states that $a^{2} + b^{2} = c^{2}$. Segment 55 gives $\hat{H}\,\Psi = E\,\Psi$.

The bound in item 56 reads $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. Rewriting item 57 yields $e^{i\pi} = -1$. One verifies item 58: $a^{2} + b^{2} = c^{2}$. For comparison, item 59 states that $\hat{H}\,\Psi = E\,\Psi$. Segment 60 gives $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$.

The bound in item 61 reads $e^{i\pi} = -1$. Rewriting item 62 yields $a^{2} + b^{2} = c^{2}$. One verifies item 63: $\hat{H}\,\Psi = E\,\Psi$. For comparison, item 64 states that $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. Segment 65 gives $e^{i\pi} = -1$.

The bound in item 66 reads $a^{2} + b^{2} = c^{2}$. Rewriting item 67 yields $\hat{H}\,\Psi = E\,\Psi$. One verifies item 68: $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. For comparison, item 69 states that $e^{i\pi} = -1$. Segment 70 gives $a^{2} + b^{2} = c^{2}$.

The bound in item 71 reads $\hat{H}\,\Psi = E\,\Psi$. Rewriting item 72 yields $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. One verifies item 73: $e^{i\pi} = -1$. For comparison, item 74 states that $a^{2} + b^{2} = c^{2}$. Segment 75 gives $\hat{H}\,\Psi = E\,\Psi$.

The bound in item 76 reads $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. Rewriting item 77 yields $e^{i\pi} = -1$. One verifies item 78: $a^{2} + b^{2} = c^{2}$. For comparison, item 79 states that $\hat{H}\,\Psi = E\,\Psi$. Segment 80 gives $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$.

The bound in item 81 reads $e^{i\pi} = -1$. Rewriting item 82 yields $a^{2} + b^{2} = c^{2}$. One verifies item 83: $\hat{H}\,\Psi = E\,\Psi$. For comparison, item 84 states that $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. Segment 85 gives $e^{i\pi} = -1$.

The bound in item 86 reads $a^{2} + b^{2} = c^{2}$. Rewriting item 87 yields $\hat{H}\,\Psi = E\,\Psi$. One verifies item 88: $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. For comparison, item 89 states that $e^{i\pi} = -1$. Segment 90 gives $a^{2} + b^{2} = c^{2}$.

The bound in item 91 reads $\hat{H}\,\Psi = E\,\Psi$. Rewriting item 92 yields $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. One verifies item 93: $e^{i\pi} = -1$. For comparison, item 94 states that $a^{2} + b^{2} = c^{2}$. Segment 95 gives $\hat{H}\,\Psi = E\,\Psi$.

The bound in item 96 reads $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$. Rewriting item 97 yields $e^{i\pi} = -1$. One verifies item 98: $a^{2} + b^{2} = c^{2}$. For comparison, item 99 states that $\hat{H}\,\Psi = E\,\Psi$. Segment 100 gives $\nabla \cdot \mathbf{E} = \rho / \varepsilon_{0}$.

## Unique display (100)

Distinct single-line display blocks; like the unique inline section, every block is a fresh typeset.

$$
\sum_{k=1}^{\infty} \frac{1}{k^{2}} = \zeta(2) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 2 & 4 & 6 \\ 3 & 4 & 5 \\ 7 & 4 & 2\end{pmatrix} = D_{2}
$$

$$
\oint_{\partial \Omega_{3}} \mathbf{F}_{3} \cdot d\mathbf{r} = 2\pi \cdot 3
$$

$$
\frac{d^{4}}{dx^{4}} \left(e^{-x^{2}}\right) = H_{4}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{5}\right] = \frac{6!}{s^{6}} \quad (n \ge 5)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{7}} = \zeta(7) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 7 & 9 & 11 \\ 8 & 14 & 10 \\ 12 & 9 & 7\end{pmatrix} = D_{7}
$$

$$
\oint_{\partial \Omega_{8}} \mathbf{F}_{8} \cdot d\mathbf{r} = 2\pi \cdot 8
$$

$$
\frac{d^{9}}{dx^{9}} \left(e^{-x^{2}}\right) = H_{9}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{10}\right] = \frac{11!}{s^{11}} \quad (n \ge 10)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{12}} = \zeta(12) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 12 & 14 & 16 \\ 13 & 24 & 15 \\ 17 & 14 & 12\end{pmatrix} = D_{12}
$$

$$
\oint_{\partial \Omega_{13}} \mathbf{F}_{13} \cdot d\mathbf{r} = 2\pi \cdot 13
$$

$$
\frac{d^{14}}{dx^{14}} \left(e^{-x^{2}}\right) = H_{14}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{15}\right] = \frac{16!}{s^{16}} \quad (n \ge 15)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{17}} = \zeta(17) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 17 & 19 & 21 \\ 18 & 34 & 20 \\ 22 & 19 & 17\end{pmatrix} = D_{17}
$$

$$
\oint_{\partial \Omega_{18}} \mathbf{F}_{18} \cdot d\mathbf{r} = 2\pi \cdot 18
$$

$$
\frac{d^{19}}{dx^{19}} \left(e^{-x^{2}}\right) = H_{19}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{20}\right] = \frac{21!}{s^{21}} \quad (n \ge 20)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{22}} = \zeta(22) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 22 & 24 & 26 \\ 23 & 44 & 25 \\ 27 & 24 & 22\end{pmatrix} = D_{22}
$$

$$
\oint_{\partial \Omega_{23}} \mathbf{F}_{23} \cdot d\mathbf{r} = 2\pi \cdot 23
$$

$$
\frac{d^{24}}{dx^{24}} \left(e^{-x^{2}}\right) = H_{24}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{25}\right] = \frac{26!}{s^{26}} \quad (n \ge 25)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{27}} = \zeta(27) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 27 & 29 & 31 \\ 28 & 54 & 30 \\ 32 & 29 & 27\end{pmatrix} = D_{27}
$$

$$
\oint_{\partial \Omega_{28}} \mathbf{F}_{28} \cdot d\mathbf{r} = 2\pi \cdot 28
$$

$$
\frac{d^{29}}{dx^{29}} \left(e^{-x^{2}}\right) = H_{29}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{30}\right] = \frac{31!}{s^{31}} \quad (n \ge 30)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{32}} = \zeta(32) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 32 & 34 & 36 \\ 33 & 64 & 35 \\ 37 & 34 & 32\end{pmatrix} = D_{32}
$$

$$
\oint_{\partial \Omega_{33}} \mathbf{F}_{33} \cdot d\mathbf{r} = 2\pi \cdot 33
$$

$$
\frac{d^{34}}{dx^{34}} \left(e^{-x^{2}}\right) = H_{34}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{35}\right] = \frac{36!}{s^{36}} \quad (n \ge 35)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{37}} = \zeta(37) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 37 & 39 & 41 \\ 38 & 74 & 40 \\ 42 & 39 & 37\end{pmatrix} = D_{37}
$$

$$
\oint_{\partial \Omega_{38}} \mathbf{F}_{38} \cdot d\mathbf{r} = 2\pi \cdot 38
$$

$$
\frac{d^{39}}{dx^{39}} \left(e^{-x^{2}}\right) = H_{39}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{40}\right] = \frac{41!}{s^{41}} \quad (n \ge 40)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{42}} = \zeta(42) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 42 & 44 & 46 \\ 43 & 84 & 45 \\ 47 & 44 & 42\end{pmatrix} = D_{42}
$$

$$
\oint_{\partial \Omega_{43}} \mathbf{F}_{43} \cdot d\mathbf{r} = 2\pi \cdot 43
$$

$$
\frac{d^{44}}{dx^{44}} \left(e^{-x^{2}}\right) = H_{44}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{45}\right] = \frac{46!}{s^{46}} \quad (n \ge 45)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{47}} = \zeta(47) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 47 & 49 & 51 \\ 48 & 94 & 50 \\ 52 & 49 & 47\end{pmatrix} = D_{47}
$$

$$
\oint_{\partial \Omega_{48}} \mathbf{F}_{48} \cdot d\mathbf{r} = 2\pi \cdot 48
$$

$$
\frac{d^{49}}{dx^{49}} \left(e^{-x^{2}}\right) = H_{49}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{50}\right] = \frac{51!}{s^{51}} \quad (n \ge 50)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{52}} = \zeta(52) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 52 & 54 & 56 \\ 53 & 104 & 55 \\ 57 & 54 & 52\end{pmatrix} = D_{52}
$$

$$
\oint_{\partial \Omega_{53}} \mathbf{F}_{53} \cdot d\mathbf{r} = 2\pi \cdot 53
$$

$$
\frac{d^{54}}{dx^{54}} \left(e^{-x^{2}}\right) = H_{54}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{55}\right] = \frac{56!}{s^{56}} \quad (n \ge 55)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{57}} = \zeta(57) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 57 & 59 & 61 \\ 58 & 114 & 60 \\ 62 & 59 & 57\end{pmatrix} = D_{57}
$$

$$
\oint_{\partial \Omega_{58}} \mathbf{F}_{58} \cdot d\mathbf{r} = 2\pi \cdot 58
$$

$$
\frac{d^{59}}{dx^{59}} \left(e^{-x^{2}}\right) = H_{59}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{60}\right] = \frac{61!}{s^{61}} \quad (n \ge 60)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{62}} = \zeta(62) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 62 & 64 & 66 \\ 63 & 124 & 65 \\ 67 & 64 & 62\end{pmatrix} = D_{62}
$$

$$
\oint_{\partial \Omega_{63}} \mathbf{F}_{63} \cdot d\mathbf{r} = 2\pi \cdot 63
$$

$$
\frac{d^{64}}{dx^{64}} \left(e^{-x^{2}}\right) = H_{64}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{65}\right] = \frac{66!}{s^{66}} \quad (n \ge 65)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{67}} = \zeta(67) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 67 & 69 & 71 \\ 68 & 134 & 70 \\ 72 & 69 & 67\end{pmatrix} = D_{67}
$$

$$
\oint_{\partial \Omega_{68}} \mathbf{F}_{68} \cdot d\mathbf{r} = 2\pi \cdot 68
$$

$$
\frac{d^{69}}{dx^{69}} \left(e^{-x^{2}}\right) = H_{69}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{70}\right] = \frac{71!}{s^{71}} \quad (n \ge 70)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{72}} = \zeta(72) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 72 & 74 & 76 \\ 73 & 144 & 75 \\ 77 & 74 & 72\end{pmatrix} = D_{72}
$$

$$
\oint_{\partial \Omega_{73}} \mathbf{F}_{73} \cdot d\mathbf{r} = 2\pi \cdot 73
$$

$$
\frac{d^{74}}{dx^{74}} \left(e^{-x^{2}}\right) = H_{74}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{75}\right] = \frac{76!}{s^{76}} \quad (n \ge 75)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{77}} = \zeta(77) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 77 & 79 & 81 \\ 78 & 154 & 80 \\ 82 & 79 & 77\end{pmatrix} = D_{77}
$$

$$
\oint_{\partial \Omega_{78}} \mathbf{F}_{78} \cdot d\mathbf{r} = 2\pi \cdot 78
$$

$$
\frac{d^{79}}{dx^{79}} \left(e^{-x^{2}}\right) = H_{79}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{80}\right] = \frac{81!}{s^{81}} \quad (n \ge 80)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{82}} = \zeta(82) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 82 & 84 & 86 \\ 83 & 164 & 85 \\ 87 & 84 & 82\end{pmatrix} = D_{82}
$$

$$
\oint_{\partial \Omega_{83}} \mathbf{F}_{83} \cdot d\mathbf{r} = 2\pi \cdot 83
$$

$$
\frac{d^{84}}{dx^{84}} \left(e^{-x^{2}}\right) = H_{84}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{85}\right] = \frac{86!}{s^{86}} \quad (n \ge 85)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{87}} = \zeta(87) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 87 & 89 & 91 \\ 88 & 174 & 90 \\ 92 & 89 & 87\end{pmatrix} = D_{87}
$$

$$
\oint_{\partial \Omega_{88}} \mathbf{F}_{88} \cdot d\mathbf{r} = 2\pi \cdot 88
$$

$$
\frac{d^{89}}{dx^{89}} \left(e^{-x^{2}}\right) = H_{89}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{90}\right] = \frac{91!}{s^{91}} \quad (n \ge 90)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{92}} = \zeta(92) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 92 & 94 & 96 \\ 93 & 184 & 95 \\ 97 & 94 & 92\end{pmatrix} = D_{92}
$$

$$
\oint_{\partial \Omega_{93}} \mathbf{F}_{93} \cdot d\mathbf{r} = 2\pi \cdot 93
$$

$$
\frac{d^{94}}{dx^{94}} \left(e^{-x^{2}}\right) = H_{94}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{95}\right] = \frac{96!}{s^{96}} \quad (n \ge 95)
$$

$$
\sum_{k=1}^{\infty} \frac{1}{k^{97}} = \zeta(97) \le \frac{\pi^{2}}{6}
$$

$$
\det\!\begin{pmatrix} 97 & 99 & 101 \\ 98 & 194 & 100 \\ 102 & 99 & 97\end{pmatrix} = D_{97}
$$

$$
\oint_{\partial \Omega_{98}} \mathbf{F}_{98} \cdot d\mathbf{r} = 2\pi \cdot 98
$$

$$
\frac{d^{99}}{dx^{99}} \left(e^{-x^{2}}\right) = H_{99}(x)\,e^{-x^{2}}
$$

$$
\mathcal{L}\!\left[t^{100}\right] = \frac{101!}{s^{101}} \quad (n \ge 100)
$$

## Repeated display (50)

Two display sources alternate, measuring repeated display typesets against the cache.

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

$$
\int_{-\infty}^{\infty} e^{-x^{2}}\,dx = \sqrt{\pi}
$$

$$
\mathbb{R}^{n} \xrightarrow{\ \sim\ } \mathbb{C}^{n}
$$

## Preamble macros (25; 20 inline, 5 display)

These formulas call macros from `benchmark-preamble.tex`; they render only when the plugin's Preamble file setting points at that file, and render as raw TeX if it does not.

A macro evaluation $\benchQuad{x_{1}}$ appears here.

A macro evaluation $\benchNorm{\vec{v}_{2}}$ appears here.

A macro evaluation $\benchSet$ appears here.

A macro evaluation $\benchSpeedOfLight$ appears here.

$$
\benchMap{f_{5}}{\mathbb{C}}
$$

A macro evaluation $\benchQuad{x_{6}}$ appears here.

A macro evaluation $\benchNorm{\vec{v}_{7}}$ appears here.

A macro evaluation $\benchSet$ appears here.

A macro evaluation $\benchSpeedOfLight$ appears here.

$$
\benchMap{f_{10}}{\mathbb{C}}
$$

A macro evaluation $\benchQuad{x_{11}}$ appears here.

A macro evaluation $\benchNorm{\vec{v}_{12}}$ appears here.

A macro evaluation $\benchSet$ appears here.

A macro evaluation $\benchSpeedOfLight$ appears here.

$$
\benchMap{f_{15}}{\mathbb{C}}
$$

A macro evaluation $\benchQuad{x_{16}}$ appears here.

A macro evaluation $\benchNorm{\vec{v}_{17}}$ appears here.

A macro evaluation $\benchSet$ appears here.

A macro evaluation $\benchSpeedOfLight$ appears here.

$$
\benchMap{f_{20}}{\mathbb{C}}
$$

A macro evaluation $\benchQuad{x_{21}}$ appears here.

A macro evaluation $\benchNorm{\vec{v}_{22}}$ appears here.

A macro evaluation $\benchSet$ appears here.

A macro evaluation $\benchSpeedOfLight$ appears here.

$$
\benchMap{f_{25}}{\mathbb{C}}
$$

## Invalid input (25; 13 inline, 12 display)

These control sequences are intentionally undefined. The plugin must leave them to Obsidian's failure fallback without disturbing neighboring formulas.

The undefined command $\benchInvalidCommand1$ must fail closed.

$$
\benchInvalidDisplay1
$$

The undefined command $\benchInvalidCommand2$ must fail closed.

$$
\benchInvalidDisplay2
$$

The undefined command $\benchInvalidCommand3$ must fail closed.

$$
\benchInvalidDisplay3
$$

The undefined command $\benchInvalidCommand4$ must fail closed.

$$
\benchInvalidDisplay4
$$

The undefined command $\benchInvalidCommand5$ must fail closed.

$$
\benchInvalidDisplay5
$$

The undefined command $\benchInvalidCommand6$ must fail closed.

$$
\benchInvalidDisplay6
$$

The undefined command $\benchInvalidCommand7$ must fail closed.

$$
\benchInvalidDisplay7
$$

The undefined command $\benchInvalidCommand8$ must fail closed.

$$
\benchInvalidDisplay8
$$

The undefined command $\benchInvalidCommand9$ must fail closed.

$$
\benchInvalidDisplay9
$$

The undefined command $\benchInvalidCommand10$ must fail closed.

$$
\benchInvalidDisplay10
$$

The undefined command $\benchInvalidCommand11$ must fail closed.

$$
\benchInvalidDisplay11
$$

The undefined command $\benchInvalidCommand12$ must fail closed.

$$
\benchInvalidDisplay12
$$

The undefined command $\benchInvalidCommand13$ must fail closed.

## Math-free anchor

This paragraph contains no mathematics. Use it for typing measurements: text entered here must not re-render any formula in the note, so edits in this paragraph isolate editor latency from render work.

