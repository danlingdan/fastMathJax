# Embed target

This note is embedded from `surface-contexts.md` (ROADMAP SURF-01). Embedded notes render
without section metadata, so their math is expected to stay on Obsidian's renderer.

Embedded inline $\omega_{6}$ math and a display formula:

$$
\oint_{\partial\Omega_{6}} \mathbf{F}_{6} \cdot d\mathbf{r} = 0
$$
