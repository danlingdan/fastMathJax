% Latest MathJax benchmark preamble (ROADMAP PERF-01 fixtures).
% Copy this file into the benchmark vault, then set the plugin's "Preamble file"
% setting to its vault-relative path. The "Preamble macros" section of the
% benchmark notes renders only when this file is configured.

\newcommand{\benchSpeedOfLight}{299\,792\,458\,\mathrm{m/s}}
\newcommand{\benchQuad}[1]{#1^{2} + 2#1 + 1}
\newcommand{\benchNorm}[1]{\left\| #1 \right\|^{2}}
\newcommand{\benchSet}{\mathbb{R}^{n}}
\newcommand{\benchMap}[2]{#1 \colon \benchSet \to #2}
